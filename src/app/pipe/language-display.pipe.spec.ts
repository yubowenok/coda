import { LanguageDisplayPipe } from './language-display.pipe';

describe('LanguageDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
