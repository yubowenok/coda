import { HljsDirective } from './hljs.directive';

describe('HljsDirective', () => {
  it('should create an instance', () => {
    const directive = new HljsDirective();
    expect(directive).toBeTruthy();
  });
});
