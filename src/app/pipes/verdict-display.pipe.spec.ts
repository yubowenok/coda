import { VerdictDisplayPipe } from './verdict-display.pipe';

describe('VerdictDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new VerdictDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
