import { CodeFlaskDirective } from './code-flask.directive';

describe('CodeFlaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeFlaskDirective();
    expect(directive).toBeTruthy();
  });
});
