coda.controller('RegisterCtrl', [
  '$scope', '$location', 'page',
  function($scope, $location, page) {
    page.setNav('register');

    /**
     * Registers the user.
     */
    $scope.register = function() {
    };
  }
]);
